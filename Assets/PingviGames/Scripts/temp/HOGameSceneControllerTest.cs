using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameSceneControllerTest : MonoBehaviour {


    private GameSceneManager _manager;
    /*
	// Use this for initialization
	void Start () {
        _manager = GetComponent<HOGameSceneManager>();
        _manager.OnItemFindedEvent += onItemFinded;
        _manager.OnNewItemActivatedEvent += onNewItemActivated;

        _manager.InitForGame(0, 40, 6, 3, -1,"");
	}

    private void onItemFinded(HOGameHiddenItem[] _items)
    {        
        Debug.Log("ItemFinded:"+_items[0].ItemName);
        _manager.RemoveFindedItems(_items);
    }

    private void onNewItemActivated(HOGameHiddenItem _item)
    {
        Debug.Log("New Item Activated");
    }
    */

	
}
